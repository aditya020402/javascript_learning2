// conversion in data types

// equality operator == and comparison >,>=,<=,< they both function differently
// comparions coverts null to a number therefore null becomes 0 

console.log(null > 0); // gives false 
console.log(null == 0); // gives false
console.log(null >=0);// gives true


// strict check === check without conversion 

